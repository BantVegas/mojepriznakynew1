# Názov aplikácie
spring.application.name=mojepriznakynew

# OpenAI API k?ú?
openai.api.key=sk-abc123yourapikey

# Mail ? SMTP konfigurácia pre Gmail (cez App Password)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.default-encoding=UTF-8

# JPA / H2 databáza
spring.datasource.url=jdbc:h2:mem:mojepriznakydb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

# H2 konzola (dev)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Charset / encoding
spring.web.encoding.charset=UTF-8
spring.web.encoding.enabled=true
spring.web.encoding.force=true

spring.mail.properties.mail.debug=true
jwt.secret=mojepriznaky-supertajny-kluc-256-bitovy-musi-byt-dostdlhy





